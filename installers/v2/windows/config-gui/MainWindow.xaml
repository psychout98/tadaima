<Window
    x:Class="Tadaima.Config.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Tadaima Setup">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Welcome to Tadaima" FontSize="24" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1"
                   Text="Pair this PC with your Tadaima instance. Copy the 6-character pairing code from the web app, then choose where downloaded media should live."
                   TextWrapping="WrapWholeWords"/>

        <TextBox Grid.Row="2"
                 x:Name="RelayUrlBox"
                 Header="Relay URL"
                 PlaceholderText="https://"/>

        <TextBox Grid.Row="3"
                 x:Name="PairingCodeBox"
                 Header="Pairing Code"
                 MaxLength="6"
                 CharacterCasing="Upper"/>

        <Grid Grid.Row="4" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="MoviesDirBox" Header="Movies Directory"/>
            <Button Grid.Column="1" VerticalAlignment="Bottom" Click="OnChooseMovies" Content="Choose…"/>
        </Grid>

        <Grid Grid.Row="5" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="TvDirBox" Header="TV Shows Directory"/>
            <Button Grid.Column="1" VerticalAlignment="Bottom" Click="OnChooseTv" Content="Choose…"/>
        </Grid>

        <TextBlock Grid.Row="6" x:Name="StatusText" Foreground="Red"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button x:Name="PairButton" Click="OnPair" Content="Pair and Save" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
